import type { RuntimeRequest, RuntimeResponse } from '../shared/types';

document.head.append(Object.assign(document.createElement('style'), { textContent: `:root{font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#20232b;background:#fff}*{box-sizing:border-box}body{width:330px;margin:0}main{padding:16px}header{display:flex;align-items:center;justify-content:space-between}.brand{display:flex;align-items:center;gap:8px;font-size:18px}.brand span{display:grid;width:27px;height:27px;place-items:center;border-radius:8px;background:#eef0ff;color:#5665ce;font-weight:800}button{border:0;border-radius:9px;background:#20232b;color:#fff;font:inherit;font-weight:650;cursor:pointer}header button{padding:7px 9px;background:#f0f1f5;color:#596071;font-size:12px}.tip,.boundary,.refresh{font-size:12px;line-height:1.55}.tip{color:#747a88;margin:17px 0 12px}.boundary{margin:16px 0 0;color:#747a88}.refresh{margin:10px 0 0;color:#9ba1ad}form{display:flex;gap:7px}input{min-width:0;flex:1;height:39px;padding:0 10px;border:1px solid #dce0e8;border-radius:9px;font:inherit;outline:none}input:focus{border-color:#5a67d8;box-shadow:0 0 0 3px #eef0ff}form button{width:64px}#result{margin-top:15px;padding:13px;border-top:1px solid #eceef3;background:#fbfbfd;font-size:13px;line-height:1.6}#result h2{margin:0 0 5px;font-size:15px}#result p{margin:5px 0;color:#555c6b}` }));

function message(request: RuntimeRequest): Promise<RuntimeResponse> { return new Promise((resolve) => chrome.runtime.sendMessage(request, (response: RuntimeResponse) => resolve(response))); }
const form = document.querySelector<HTMLFormElement>('#lookup')!; const term = document.querySelector<HTMLInputElement>('#term')!; const result = document.querySelector<HTMLElement>('#result')!;
document.querySelector<HTMLButtonElement>('#settings')!.addEventListener('click', () => chrome.runtime.openOptionsPage());
form.addEventListener('submit', async (event) => { event.preventDefault(); const text = term.value.trim(); if (!text) return; const button = form.querySelector<HTMLButtonElement>('button')!; button.disabled = true; button.textContent = '解读中…'; result.hidden = false; result.textContent = '正在生成解释…'; const response = await message({ type: 'LOOKUP_CORE', preference: 'auto', draft: { term: text, context: text, selectionMode: 'term', rect: { left: 0, top: 0, right: 0, bottom: 0 } } }); button.disabled = false; button.textContent = '解读'; if (response.ok && 'result' in response) { result.replaceChildren(); const heading = document.createElement('h2'); heading.textContent = response.result.canonicalNameZh || response.result.canonicalNameEn || text; const oneLine = document.createElement('p'); oneLine.textContent = response.result.oneLine; const brief = document.createElement('p'); brief.textContent = response.result.briefIntro; result.append(heading, oneLine, brief); } else result.textContent = !response.ok ? response.error : '返回内容异常，请重试。'; });
